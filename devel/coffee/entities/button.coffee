
class Button extends PIXI.Sprite
    constructor: ->
        @width = 0
        @height = 0

    update: (deltaTime) ->
        return

    press: ->
        return

    boundingbox: ->
        return

    setPriority: ->
        return

    getPriority: ->
        return
