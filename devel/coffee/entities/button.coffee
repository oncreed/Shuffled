globals = require 'sh-globals'

class Button extends PIXI.Sprite
    constructor: ->
        @width = 0
        @height = 0

    update: (deltaTime) ->
        return

    press: ->
        return

    getBoundingBox: ->
        return

    setRenderPriority: ->
        return

    getRenderPriority: ->
        return

module.exports = Button
